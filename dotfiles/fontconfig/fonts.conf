<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
  <!-- Standard user font directories -->
  <dir>~/.local/share/fonts</dir>
  <dir>~/.fonts</dir>

  <!-- Add Nix profile fonts -->
  <dir>~/.nix-profile/share/fonts</dir>
</fontconfig>
