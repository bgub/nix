# Enable Ghostty's SSH compatibility wrapper so remote sessions get a valid TERM.
shell-integration-features = ssh-env,ssh-terminfo

# COSMIC handles window layout, so keep terminal chrome out of the way.
window-decoration = none
